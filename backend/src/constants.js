const Joi = require('joi');

const INITIAL_CLIENT_COUNT = 8000;

const SAMPLE_HEADERS = ["id",  "stat1", "stat2", "stat3", "stat4", "stat5", "stat6", "name", "ip", "fw", "parentId", "geoLat", "geoLong"]

const HEADER_SCHEMA =

module.exports = {
    INITIAL_CLIENT_COUNT,
    SAMPLE_HEADERS
}